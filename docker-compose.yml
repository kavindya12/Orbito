services:
  postgres:
    image: postgres:16-alpine
    container_name: orbito-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: orbito
      POSTGRES_PASSWORD: orbito
      POSTGRES_DB: orbito
    ports:
      - "5432:5432"
    volumes:
      - orbito_pg:/var/lib/postgresql/data

volumes:
  orbito_pg:
